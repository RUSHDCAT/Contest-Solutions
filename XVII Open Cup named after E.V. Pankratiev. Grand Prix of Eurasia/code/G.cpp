#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
const int N = 100 + 10;
int n, p[N];
char s[N][N];

struct Nod {
	int x, y;
}
int main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; i ++) scanf("%s", s[i] + 1);
	for (int i = 1; i <= n; i ++) scanf("%d", &p[i]);

	for (int i = 1; i <= n; i ++) {
		nod[i].x = 0;
		for (int j = 1; j <= n; j ++) {

		}
	} 
}